#pragma once

#include"Point.h"

double findNearestPointsInNLgN(Point*, int, Point*);

double findNearestPointsInN2(Point*, int, Point*);

double findNearestPoints(Point*, int, int, Point*);

int compareX(Point, Point);

int compareY(Point, Point);

double getDistance(Point, Point);